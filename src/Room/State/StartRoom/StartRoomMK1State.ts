import {RoomState} from "../RoomState";
import {StartRoom} from "../../Types/StartRoom";

export class StartRoomMK1State implements RoomState{
  enter(object: StartRoom): void {
  }

  execute(object: StartRoom): void {
  }

  exit(object: StartRoom): void {
  }

}
